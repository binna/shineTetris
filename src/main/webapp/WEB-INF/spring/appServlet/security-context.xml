<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">
		
	<!-- 컨테이너의 HTTP security 요소 -->
	<security:http>
		<!-- ROLE_MEMBER 라는 권한이 있는 사용자만이 접근 가능 -->
		<security:intercept-url pattern="/login/loginAfter" access="hasRole('ROLE_MEMBER')"/>
		
		
		
		
		<!-- 로그인 -->
		<security:form-login login-page="/login/loginMain"/>
		
		<!-- 접근 제한 메시지 처리 -->
		<security:access-denied-handler ref="/login/AccessDenied"/>
		
		<!-- 로그아웃 -->
		<security:logout logout-url="/login/logout" invalidate-session="true" delete-cookies="JSESSIONID"/>
		
	</security:http>

	<!-- Spring Security가 동작하기 위해서는 AuthenticationManager가 필요, AuthenticationManager 빈 생성 -->
	<security:authentication-manager>
		<!-- UserDetailsService 사용한 인증과 권한 처리 -->
		<security:authentication-provider>
			<security:user-service>
				<security:user name="member" password="{noop}member" authorities="ROLE_MEMBER"/>
			</security:user-service>
		</security:authentication-provider>
	</security:authentication-manager>

</beans>